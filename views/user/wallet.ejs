<%- include("../partials/header.ejs") %>


<br>
<nav class="text-sm px-4 sm:ml-6 font-medium text-gray-700 mb-4 sm:mb-6">
    <ol class="flex items-center space-x-2">
      <li>
        <a href="/home" class="text-blue-600 hover:text-blue-700">Home</a>
      </li>
      <li>/</li>
      <li>
        <a href="/profile" class="text-blue-600 hover:text-blue-700">Profile</a>
      </li>
      <li>/</li>
      <li class="text-gray-400">Wallet</li>
    </ol>
  </nav>
  
  <div class="container mx-auto my-6 sm:my-10 px-4 sm:px-6 lg:px-8">
    <div class="bg-gradient-to-br from-white to-blue-50 shadow-lg sm:shadow-2xl rounded-xl overflow-hidden">
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-4 sm:gap-6 p-4 sm:p-6">
     
        <div class="lg:hidden mb-6">
          <button id="mobileMenuButton" class="w-full flex justify-between items-center p-4 bg-white border border-blue-200 rounded-lg shadow-sm hover:bg-blue-50 transition-colors">
            <span class="font-medium text-gray-700">Menu</span>
            <i class="fas fa-chevron-down text-blue-500 transition-transform duration-300" id="mobileMenuIcon"></i>
          </button>
          <div id="mobileMenuContent" class="hidden mt-2 bg-white border border-blue-100 rounded-lg shadow-md overflow-hidden">
            <ul class="space-y-1">
              <li>
                <a href="/profile" class="flex items-center p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50">
                  <i class="fas fa-user text-blue-500 mr-4"></i>
                  <span class="text-gray-700">Profile</span>
                </a>
              </li>
              <li>
                <a href="/addressbook" class="flex items-center p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50">
                  <i class="fas fa-map-marker-alt text-blue-500 mr-4"></i>
                  <span class="text-gray-700">Address</span>
                </a>
              </li>
              <li>
                <a href="/wallet" class="flex items-center p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50">
                  <i class="fas fa-wallet text-blue-500 mr-4"></i>
                  <span class="text-gray-700">Wallet</span>
                </a>
              </li>
              <li>
                <a href="/orderlist" class="flex items-center p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50">
                  <i class="fas fa-box text-blue-500 mr-4"></i>
                  <span class="text-gray-700">Order</span>
                </a>
              </li>
              <li>
                <a href="/logout" class="flex items-center p-4 rounded-lg group transition-all duration-300 hover:bg-red-50">
                  <i class="fas fa-sign-out-alt text-red-500 mr-4"></i>
                  <span class="text-gray-700">Logout</span>
                </a>
              </li>
            </ul>
          </div>
        </div>
  
       
        <div class="hidden lg:block col-span-1 bg-white border border-blue-100 p-4 sm:p-6 rounded-xl shadow-lg">
          <ul class="space-y-3 sm:space-y-4">
            <li>
              <a href="/profile" class="flex items-center p-3 sm:p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50 hover:shadow-md">
                <i class="fas fa-user text-blue-500 text-sm sm:text-base mr-3 sm:mr-4 group-hover:scale-110 group-hover:rotate-6"></i>
                <span class="text-sm sm:text-base text-gray-700 group-hover:text-blue-600 group-hover:font-medium">Profile</span>
              </a>
            </li>
            <li>
              <a href="/addressbook" class="flex items-center p-3 sm:p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50 hover:shadow-md">
                <i class="fas fa-map-marker-alt text-blue-500 text-sm sm:text-base mr-3 sm:mr-4 group-hover:scale-110 group-hover:rotate-6"></i>
                <span class="text-sm sm:text-base text-gray-700 group-hover:text-blue-600 group-hover:font-medium">Address</span>
              </a>
            </li>
            <li>
              <a href="/wallet" class="flex items-center p-3 sm:p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50 hover:shadow-md">
                <i class="fas fa-wallet text-blue-500 text-sm sm:text-base mr-3 sm:mr-4 group-hover:scale-110 group-hover:rotate-6"></i>
                <span class="text-sm sm:text-base text-gray-700 group-hover:text-blue-600 group-hover:font-medium">Wallet</span>
              </a>
            </li>
            <li>
              <a href="/orderlist" class="flex items-center p-3 sm:p-4 rounded-lg group transition-all duration-300 hover:bg-blue-50 hover:shadow-md">
                <i class="fas fa-box text-blue-500 text-sm sm:text-base mr-3 sm:mr-4 group-hover:scale-110 group-hover:rotate-6"></i>
                <span class="text-sm sm:text-base text-gray-700 group-hover:text-blue-600 group-hover:font-medium">Order</span>
              </a>
            </li>
            <li>
              <a href="/logout" class="flex items-center p-3 sm:p-4 rounded-lg group transition-all duration-300 hover:bg-red-50 hover:shadow-md">
                <i class="fas fa-sign-out-alt text-red-500 text-sm sm:text-base mr-3 sm:mr-4 group-hover:scale-110 group-hover:rotate-6"></i>
                <span class="text-sm sm:text-base text-gray-700 group-hover:text-red-600 group-hover:font-medium">Logout</span>
              </a>
            </li>
          </ul>
        </div>
  
        
        <div class="col-span-3 space-y-4 sm:space-y-6">
        
          <div class="bg-white border border-blue-100 p-4 sm:p-6 rounded-xl shadow-md sm:shadow-lg">
            <div class="w-24 h-24 sm:w-32 sm:h-32 bg-red-400 rounded-full mx-auto flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" viewBox="0 0 24 24" class="w-12 h-12 sm:w-16 sm:h-16 text-white">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
            </div>
            <p class="text-center mt-3 sm:mt-4 text-base sm:text-lg font-semibold"><%= userName %></p>
          </div>
  
          
          <div class="flex flex-col sm:flex-row items-center justify-between gap-3 sm:gap-4">
           
            <div class="text-white text-center py-2 sm:py-3 rounded-lg w-full sm:w-1/2 bg-gray-800">
              <p class="mb-1 sm:mb-2 text-sm sm:text-base">Current Balance:</p>
              <h2 class="font-bold text-xl sm:text-2xl">₹ <%= wallet.balance.toFixed(2) %></h2>
            </div>
            
          
           
          </div>
  
         
          <div class="mt-4 sm:mt-5">
            <h3 class="text-lg sm:text-xl font-semibold mb-2 sm:mb-3">Transaction History</h3>
            <div class="overflow-x-auto">
              <table class="min-w-full text-left border-collapse">
                <thead>
                  <tr class="bg-gray-200">
                    <th class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">Date</th>
                    <th class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">Type</th>
                    <th class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">Amount</th>
                  </tr>
                </thead>
                <tbody id="transactions-table">
                 
                </tbody>
              </table>
            </div>
            <div class="flex justify-center items-center mt-3 sm:mt-4">
              <button id="prev-btn" class="px-3 py-1 sm:px-4 sm:py-2 bg-gray-300 rounded-lg mr-2 text-sm sm:text-base disabled:opacity-50" disabled>Previous</button>
              <span id="page-info" class="px-3 py-1 sm:px-4 sm:py-2 text-sm sm:text-base">Page 1 of 1</span>
              <button id="next-btn" class="px-3 py-1 sm:px-4 sm:py-2 bg-gray-300 rounded-lg ml-2 text-sm sm:text-base disabled:opacity-50" disabled>Next</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  

  
    

<%- include("../partials/footer.ejs") %>

<script>
 
     document.getElementById('mobileMenuButton').addEventListener('click', function() {
      const menuContent = document.getElementById('mobileMenuContent');
      const menuIcon = document.getElementById('mobileMenuIcon');
      
      menuContent.classList.toggle('hidden');
      menuIcon.classList.toggle('fa-chevron-down');
      menuIcon.classList.toggle('fa-chevron-up');
    });
  
   
    document.addEventListener('DOMContentLoaded', function() {
      const transactions = [
        <% wallet.transactions.forEach(transaction => { %>
          {
            date: new Date('<%= transaction.date %>'),
            type: '<%= transaction.type %>',
            amount: <%= transaction.amount %>
          },
        <% }); %>
      ];
  
      const itemsPerPage = 5;
      let currentPage = 1;
      const totalPages = Math.ceil(transactions.length / itemsPerPage);
      const transactionsTable = document.getElementById('transactions-table');
      const prevBtn = document.getElementById('prev-btn');
      const nextBtn = document.getElementById('next-btn');
      const pageInfo = document.getElementById('page-info');
  
      function renderTransactions() {
      
        transactionsTable.innerHTML = '';
  
       
        const startIndex = (currentPage - 1) * itemsPerPage;
        const endIndex = Math.min(startIndex + itemsPerPage, transactions.length);
  
       
        for (let i = startIndex; i < endIndex; i++) {
          const transaction = transactions[i];
          const row = document.createElement('tr');
          row.className = 'border-b';
          row.innerHTML = `
            <td class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">${transaction.date.toLocaleDateString()}</td>
            <td class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">${transaction.type}</td>
            <td class="px-3 py-2 sm:px-4 sm:py-2 text-sm sm:text-base">₹ ${transaction.amount.toFixed(2)}</td>
          `;
          transactionsTable.appendChild(row);
        }
  
        
        pageInfo.textContent = `Page ${currentPage} of ${totalPages}`;
        prevBtn.disabled = currentPage === 1;
        nextBtn.disabled = currentPage === totalPages;
      }
  
    
      prevBtn.addEventListener('click', function() {
        if (currentPage > 1) {
          currentPage--;
          renderTransactions();
        }
      });
  
      nextBtn.addEventListener('click', function() {
        if (currentPage < totalPages) {
          currentPage++;
          renderTransactions();
        }
      });
  
     
      renderTransactions();
    });

document.addEventListener("DOMContentLoaded", function () {
        const rowsPerPage = 5; 
        const tableBody = document.getElementById("wallet-table-body");
        const rows = tableBody.querySelectorAll("tr");
        const totalRows = rows.length;
        const totalPages = Math.ceil(totalRows / rowsPerPage);

        let currentPage = 1;

        function updateTable() {
            
            rows.forEach((row, index) => {
                row.style.display = "none";
            });

            
            const start = (currentPage - 1) * rowsPerPage;
            const end = start + rowsPerPage;
            for (let i = start; i < end && i < totalRows; i++) {
                rows[i].style.display = "";
            }

          
            document.getElementById("page-info").innerText = `Page ${currentPage} of ${totalPages}`;

            
            document.getElementById("prev-btn").disabled = currentPage === 1;
            document.getElementById("next-btn").disabled = currentPage === totalPages;
        }

    
        document.getElementById("prev-btn").addEventListener("click", function () {
            if (currentPage > 1) {
                currentPage--;
                updateTable();
            }
        });

        document.getElementById("next-btn").addEventListener("click", function () {
            if (currentPage < totalPages) {
                currentPage++;
                updateTable();
            }
        });

        
        updateTable();
    });

</script>